{
  "name": "beauty-of-words-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "start": "npm start --prefix backend",
    "install-all": "npm install --prefix frontend && npm install --prefix backend"
  }
}
